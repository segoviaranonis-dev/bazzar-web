/**
 * Sin skeleton de grilla: con loading.tsx agresivo Next reemplaza el segment
 * y el usuario ve vacío >1s. Null = se mantiene la UI anterior hasta el RSC nuevo.
 */
export default function CatalogoLoading() {
  return null
}
